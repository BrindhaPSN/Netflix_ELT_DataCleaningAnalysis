# Importing Netflix movie data into python
    -   Loading the data into MS SQL Server table
    -   Perform Cleaning in SQL
    -   Analysis using SQL Queries

# Before loading data into SQL
    - Check the length of each column in the data through python
    - Create table in SQL based on the analysis in Python for column type and size

# Data in SQL:
    - Data cleaning
    - Data Analysis

python3 --version

to excecute : python filename.py

# Create a virtual environment
python3 -m venv netflixEnv

# Activate the environment
source netflixEnv/bin/Activate

# Install pandas,pymssql,sqlalchemy

pip install pandas,sqlalchemy,pymssql

# use pip freeze - to view installed packages